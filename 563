text = input("Введіть текст в одному рядку: ")

words = text.lower().split()

word_counts = {}

results = []

for word in words:
    current_count = word_counts.get(word, 0)
    
    results.append(current_count)
    
    word_counts[word] = current_count + 1

print(results)
